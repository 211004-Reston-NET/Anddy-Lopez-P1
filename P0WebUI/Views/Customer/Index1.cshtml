@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@* This tells you that this view will expect to retrieve a list of CustomerVM *@
@model IEnumerable<P0WebUI.Models.CustomerVM>

<h1 class="display-4">Select a Customer</h1>

<table class="table table-striped table-hover">
    <tr>
        <th>Name</th>
        <th>Address</th>
        <th>Email</th>
        <th>Phone Number</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelitem => item.Name)</td>
            <td>@Html.DisplayFor(modelitem => item.Address)</td>
            <td>@Html.DisplayFor(modelitem => item.Email)</td>
            <td>@Html.DisplayFor(modelitem => item.PhoneNumber)</td>
            <td>
                <button class="btn btn-primary">@Html.ActionLink("Select", "Select", "Customer", new { p_id = item.Id }, new { @style = "color:white;" })</button>
                <!--                                                                                    ^ this must be the same as in the controller
                                                                                                        doesn't have to be the same as BL tho -->
            </td>
        </tr>
    }
</table>